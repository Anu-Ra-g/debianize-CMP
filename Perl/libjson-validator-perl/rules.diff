--- libjson-validator-perl.debianize/debian/rules	2026-06-12 20:24:18.603160617 +0530
+++ libjson-validator-perl/debian/rules	2026-06-12 20:24:36.329512204 +0530
@@ -1,3 +1,10 @@
 #!/usr/bin/make -f
+
+SKIP_TESTS = $(shell cat debian/tests/pkg-perl/smoke-skip)
+TEST_FILES = $(filter-out $(SKIP_TESTS), $(wildcard t/*.t))
+
 %:
-	dh $@ --buildsystem=makefile
+	dh $@
+
+override_dh_auto_test:
+	RES_OPTIONS=attempts:0 http_proxy= dh_auto_test -- TEST_FILES="$(TEST_FILES)"
